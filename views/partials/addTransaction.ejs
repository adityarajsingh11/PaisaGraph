<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Transaction Form</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
    
    body {
      font-family: 'Inter', sans-serif;
    }
    
    .form-card {
      background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
      border: 2px solid #e2e8f0;
      transition: all 0.3s ease;
    }
    
    .form-card:hover {
      border-color: #cbd5e1;
      box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.1);
    }
    
    .input-group {
      position: relative;
    }
    
    .form-input {
      transition: all 0.3s ease;
      border: 2px solid #e2e8f0;
    }
    
    .form-input:focus {
      outline: none;
      border-color: #3b82f6;
      box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
      transform: translateY(-2px);
    }
    
    .form-input:hover {
      border-color: #cbd5e1;
    }
    
    .input-icon {
      position: absolute;
      left: 12px;
      top: 50%;
      transform: translateY(-50%);
      pointer-events: none;
      transition: all 0.3s ease;
    }
    
    .form-input:focus ~ .input-icon {
      color: #3b82f6;
      transform: translateY(-50%) scale(1.1);
    }
    
    .type-selector {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0.75rem;
      margin-bottom: 1rem;
    }
    
    .type-option {
      position: relative;
      cursor: pointer;
    }
    
    .type-option input[type="radio"] {
      position: absolute;
      opacity: 0;
    }
    
    .type-label {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 0.875rem;
      border: 2px solid #e2e8f0;
      border-radius: 0.75rem;
      transition: all 0.3s ease;
      font-weight: 600;
      font-size: 0.875rem;
    }
    
    .type-option input[type="radio"]:checked ~ .type-label.income-label {
      background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
      border-color: #10b981;
      color: #065f46;
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }
    
    .type-option input[type="radio"]:checked ~ .type-label.expense-label {
      background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%);
      border-color: #ef4444;
      color: #991b1b;
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }
    
    .type-label:hover {
      border-color: #cbd5e1;
      transform: translateY(-2px);
    }
    
    .submit-btn {
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }
    
    .submit-btn::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }
    
    .submit-btn:hover::before {
      width: 300px;
      height: 300px;
    }
    
    .submit-btn:hover {
      background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
      transform: translateY(-2px);
      box-shadow: 0 12px 24px rgba(59, 130, 246, 0.4);
    }
    
    .submit-btn:active {
      transform: translateY(0);
    }
    
    .category-quick-select {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
      margin-top: 0.5rem;
    }
    
    .category-chip {
      padding: 0.375rem 0.75rem;
      background: #f1f5f9;
      border: 1px solid #e2e8f0;
      border-radius: 9999px;
      font-size: 0.75rem;
      cursor: pointer;
      transition: all 0.2s ease;
    }
    
    .category-chip:hover {
      background: #e2e8f0;
      border-color: #cbd5e1;
      transform: scale(1.05);
    }
    
    .section-divider {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin: 1.5rem 0;
    }
    
    .section-divider::before,
    .section-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: linear-gradient(to right, transparent, #e2e8f0, transparent);
    }
    
    .section-divider span {
      font-size: 0.75rem;
      font-weight: 600;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .animate-slide-in {
      animation: slideIn 0.5s ease-out;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-slate-50 to-slate-100 p-6">

  <div class="form-card rounded-2xl shadow-lg p-8 max-w-2xl mx-auto animate-slide-in">
    
    <!-- Header -->
    <div class="flex items-center gap-3 mb-6">
      <div class="bg-gradient-to-br from-blue-500 to-purple-600 p-3 rounded-xl shadow-lg">
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
        </svg>
      </div>
      <div>
        <h3 class="text-2xl font-bold text-gray-800">Add Transaction</h3>
        <p class="text-sm text-gray-500">Record your income or expense</p>
      </div>
    </div>

    <form id="addForm" method="post" action="/api/transactions/add" class="space-y-5">
      
      <!-- Transaction Type Selector -->
      <div>
        <label class="block text-xs font-semibold text-gray-600 uppercase tracking-wide mb-2">Transaction Type</label>
        <div class="type-selector">
          <div class="type-option">
            <input type="radio" name="type" value="income" id="type-income" checked>
            <label for="type-income" class="type-label income-label">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
              </svg>
              Income
            </label>
          </div>
          <div class="type-option">
            <input type="radio" name="type" value="expense" id="type-expense">
            <label for="type-expense" class="type-label expense-label">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 17h8m0 0v-8m0 8l-8-8-4 4-6-6"></path>
              </svg>
              Expense
            </label>
          </div>
        </div>
      </div>

      <!-- Title Input -->
      <div class="input-group">
        <label class="block text-xs font-semibold text-gray-600 uppercase tracking-wide mb-2">Title</label>
        <div class="relative">
          <input 
            name="title" 
            placeholder="e.g., Grocery Shopping, Salary" 
            required 
            class="form-input rounded-xl p-3 pl-11 w-full text-gray-800 font-medium"
          >
          <svg class="input-icon w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"></path>
          </svg>
        </div>
      </div>

      <!-- Amount Input -->
      <div class="input-group">
        <label class="block text-xs font-semibold text-gray-600 uppercase tracking-wide mb-2">Amount</label>
        <div class="relative">
          <input 
            name="amount" 
            placeholder="0.00" 
            type="number" 
            step="0.01"
            required 
            class="form-input rounded-xl p-3 pl-11 w-full text-gray-800 font-medium text-lg"
          >
          <svg class="input-icon w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
      </div>

      <div class="section-divider">
        <span>Additional Details</span>
      </div>

      <!-- Category Input -->
      <div class="input-group">
        <label class="block text-xs font-semibold text-gray-600 uppercase tracking-wide mb-2">Category</label>
        <div class="relative">
          <input 
            name="category" 
            placeholder="e.g., Food, Transportation" 
            class="form-input rounded-xl p-3 pl-11 w-full text-gray-800 font-medium"
            id="categoryInput"
          >
          <svg class="input-icon w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
          </svg>
        </div>
        <div class="category-quick-select">
          <span class="category-chip" onclick="document.getElementById('categoryInput').value='💼 Salary'">💼 Salary</span>
          <span class="category-chip" onclick="document.getElementById('categoryInput').value='🍔 Food'">🍔 Food</span>
          <span class="category-chip" onclick="document.getElementById('categoryInput').value='💡 Utilities'">💡 Utilities</span>
          <span class="category-chip" onclick="document.getElementById('categoryInput').value='🚗 Transport'">🚗 Transport</span>
          <span class="category-chip" onclick="document.getElementById('categoryInput').value='🎮 Entertainment'">🎮 Entertainment</span>
          <span class="category-chip" onclick="document.getElementById('categoryInput').value='🏥 Healthcare'">🏥 Healthcare</span>
        </div>
      </div>

      <!-- Date Input -->
      <div class="input-group">
        <label class="block text-xs font-semibold text-gray-600 uppercase tracking-wide mb-2">Date</label>
        <div class="relative">
          <input 
            name="date" 
            type="date" 
            class="form-input rounded-xl p-3 pl-11 w-full text-gray-800 font-medium"
          >
          <svg class="input-icon w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
        </div>
      </div>

      <!-- Description Textarea -->
      <div class="input-group">
        <label class="block text-xs font-semibold text-gray-600 uppercase tracking-wide mb-2">Notes (Optional)</label>
        <div class="relative">
          <textarea 
            name="description" 
            placeholder="Add any additional details..." 
            rows="3"
            class="form-input rounded-xl p-3 pl-11 w-full text-gray-800 font-medium resize-none"
          ></textarea>
          <svg class="input-icon w-5 h-5 text-gray-400" style="top: 1rem;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
          </svg>
        </div>
      </div>

      <!-- Submit Button -->
      <button type="submit" class="submit-btn text-white w-full py-3.5 rounded-xl font-semibold shadow-lg relative z-10 flex items-center justify-center gap-2 text-base">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
        </svg>
        Add Transaction
      </button>
    </form>
  </div>

</body>
</html>